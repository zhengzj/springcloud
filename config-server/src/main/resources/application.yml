eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhengzj/springcloud
          search-paths: config
  application:
    name: config-server
